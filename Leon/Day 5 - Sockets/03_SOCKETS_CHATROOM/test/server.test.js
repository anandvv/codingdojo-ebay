const assert = require('chai').assert;
const expect = require('chai').expect;

describe('Server', function() {
  it('Testing assertion', () => {
    let myAssert = 'Hello World';
    assert.equal( myAssert, 'Hello World');
  })
})
